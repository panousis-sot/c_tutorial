#include <stdio.h>

int main() {
    int numbers[5];

    numbers[0] = 10;
    numbers[4] = 50;

    printf("numbers[0]: %d\n", numbers[0]);
    printf("numbers[4]: %d\n", numbers[4]);

    return 0;
}
